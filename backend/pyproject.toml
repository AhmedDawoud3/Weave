[project]
name = "weave-backend"
version = "0.1.0"
description = "PyTorch Execution Engine for Weave Neural Network Editor"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "torch",
    "torchvision",
    "numpy",
]

[tool.uv]
dev-dependencies = []
